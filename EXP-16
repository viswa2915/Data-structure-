#include <stdio.h>
#define SIZE 10

int hashTable[SIZE];

void insert(int key){
    int index = key % SIZE;
    while(hashTable[index] != 0)
        index = (index+1)%SIZE;
    hashTable[index] = key;
}

int main(){
    insert(10);
    insert(20);
    insert(30);

    for(int i=0;i<SIZE;i++)
        printf("%d ", hashTable[i]);

    return 0;
}
